{% extends 'templates/_document.twig' %}

{% block content %}
  <article class="post squares">
    {{ renderComponent('BlockInterviewHeader', { post: post }) }}
    {% for component in post.meta('interviewComponents') %}
      {{ renderComponent(component) }}
    {% endfor %}
  </article>
{% endblock %}
