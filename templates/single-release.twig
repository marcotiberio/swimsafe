{% extends 'templates/_document.twig' %}

{% block content %}
  <article class="post squares">
    {{ renderComponent('BlockReleaseHeader', { post: post }) }}
    {% for component in post.meta('releaseComponents') %}
      {{ renderComponent(component) }}
    {% endfor %}
  </article>
{% endblock %}
